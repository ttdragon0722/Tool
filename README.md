# Tool
選課工具
``` javascript
javascript: void (function (d) {    for (var num = 1; num < 13; num++) {        document.getElementsByName("ans"+String(num))[0].checked=true; window.scrollTo(0, document.body.scrollHeight || document.documentElement.scrollHeight); }})(document)
```